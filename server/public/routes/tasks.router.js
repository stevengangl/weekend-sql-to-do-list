const router = require('express').Router();//getting express again so we can use it in this file
const pool = require('../modules/pool');//this links the pool to the server thru this router


module.exports = router;//this is so the server can access this
